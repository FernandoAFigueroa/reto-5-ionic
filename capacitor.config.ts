import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'reto-5-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
